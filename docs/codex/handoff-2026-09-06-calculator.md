# Calculator material-profile continuation — 2026-09-06

Base `714193bea52e4a6413e318e69f0b4b56916370b6`, local branch `codex/calculator-profile-parity-20260906`; uncommitted, undeployed. Existing unrelated untracked work is preserved. The owner authorized this bounded local compatibility change alongside the plugin, and at most six small existing-VPS slice attempts, one at a time. No VPS code/profile/configuration changes, restart, install, deploy or shared cleanup were authorized or performed.

`GET /profiles` stays v2. `GET /profiles?contract=material-v1` selects v3, with a distinct catalogue digest/ETag and `material_profiles` on generic Prusa FDM rows. Each supported material is compiled with the same runtime helper and injected density; the nested entry includes material, supports=true, density and effective-profile hash. Runtime digest semantics are unchanged. Infill remains request identity; changing density or supports changes the profile hash. See `docs/integration-guide.md` for the consumer contract.

The plugin resolves the configured material variant before cache lookup and checks the returned engine/profile identity. It accepts canonical infill percentage strings such as `20%`, while rejecting mismatches and malformed values. Legacy generic v2 cannot authorize this new material-specific price path. This creates an explicit deployment dependency: do not claim that installing only the new plugin makes an old VPS compatible. No deployment has occurred.

Local validation: 2708 JavaScript tests passed, including 24 material/runtime parity combinations, density/support mutation rejection, independent ETags, v2 compatibility and v3 absence failing closed. A private route harness used the installed Windows Prusa 2.7.2+win64-202402291307 binary, repository request handler/compiler, private temporary directories and synthetic STL files of 684/12884 bytes. Two successful fresh native runs became verified plugin facts and isolated physical Woo order29; both admin model SHA-256 values match. This is not the production 2.8.1 container or normal three-engine bootstrap. Python trimesh is unavailable locally, so the existing orientation fallback was used; no CAD/automatic-orientation claim follows.

The existing `.env` endpoint rejected `/health`, `/profiles` and documented `/openapi.json` connections with ECONNREFUSED. No usable slice-service credential was found there. Remote slice attempts: **0/6**, technical access/configuration blocker, not missing permission. The actual endpoint/key are not printed or packaged. No invented alternate endpoint or credential was tried. The plugin task's durable `remote-budget.json` is the common counter; its private HTTP guard reserves attempts before transport and suspends on timeout/load/server failure.

Evidence and source deltas are delivered with the plugin's `docs/repairs/rocket3d-calculator-implementation-2026-09-06.md` and transferable evidence ZIP. Earlier local harness failures (missing isolated output directory, followed by the plugin's infill-format rejection) remain visible; they do not count as VPS attempts. Read the final report for all checks and timings, not historical counts here.

Next dependent step requires the location of the existing current VPS consumer configuration. Retain the six-attempt common counter and verify live health/catalogue/version before sending any model. If v3 is absent or mismatched, stop automatic pricing; prepare/approve any deployment separately. Existing HU checkout integrations and machine/profile business approval are outside this technical proof.
