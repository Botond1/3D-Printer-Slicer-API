# GPT-5.6-sol execution prompt — S0.1 validation truthfulness and dependency gate

You are the implementation agent for the standalone `3D-Printer-Slicer-API` repository. Work as a senior security engineer, Node.js/Python maintainer, test architect, and supply-chain maintainer. This is a live-cloud-adjacent repository: a push or merge to `main` may activate the existing VPS deployment workflow. This task authorizes local changes and local verification only. It does not authorize a push, PR, release, deployment, VPS access, or any remote/production API or slicer call.

## Why this repair stage exists

The S0 checkpoint at `b00775cd29851315564a245ab9fd8e3b4f015668` is useful and its runner exit-code fixes are correct, but architect review found several gates that can remain green without proving what they claim. The production dependency audit also reported currently fixable advisories, including high-severity Multer upload findings. Do not start the broad S1/S3 implementation yet. First turn S0 into a truthful, commit-anchored baseline and remove the known production dependency advisories.

## Exact baseline and isolation

- `S0_BASELINE`: `b00775cd29851315564a245ab9fd8e3b4f015668`.
- Start from the clean architect commit containing only this prompt on top of `S0_BASELINE`.
- Record `WORK_BASELINE` before creating the implementation branch. The only allowed path in `S0_BASELINE..WORK_BASELINE` is `prompts/codex/S0.1-validation-and-dependency-gate.md`.
- Treat the checkout containing the architect prompt as read-only. Create a new isolated linked worktree on branch `codex/s0-1-validation-dependency-gate`, based exactly on `WORK_BASELINE`. Use a verified-absent worktree path. If that branch or path already exists, stop rather than reusing or deleting it.
- In the implementation worktree require: exact branch, exact starting HEAD, `git status --porcelain=v1 -uall` empty, expected remote/repository root, and the one-path baseline diff above.
- If any preflight condition fails, return `BLOCKED`. Do not stash, reset, clean, overwrite, absorb, or delete unrelated work.
- Never edit `main`; never fetch, pull, push, open a PR, tag, release, deploy, SSH to a VPS, or touch the LeadPilot repository.
- Do not create or use real secrets, customer models, private fixtures, pricing mutations, slicer profiles, or runtime artifacts.

## Required reading and evidence order

Read completely before editing:

1. this prompt and root `AGENTS.md`;
2. `docs/codex/project-map.md`, `docs/codex/security-model.md`, `docs/codex/hardening-plan.md`;
3. all root/folder-local applicable `CLAUDE.md` and `.github/instructions/**` files;
4. every file changed by S0 (`S0_BASELINE^..S0_BASELINE` and its parent documentation commit);
5. `package.json`, `package-lock.json`, `.github/workflows/ci.yml`, `.github/workflows/deploy.yml`;
6. the auth middleware, error mapping, queue, command execution, multipart setup, validation scripts, and every current unit test.

Current instructions and executable source outrank knowledge-file claims. Treat the S0 project/security matrices as historical snapshots until you reconcile them below. Verify every architect finding against code; do not implement a claim merely because this prompt states it.

Use parallel read-only sub-agents if available for: (A) validation/scanner review, (B) test-proof review, and (C) dependency/documentation review. Reconcile their evidence centrally. Editing ownership must remain disjoint.

## Scope and strict change budget

This is a narrow baseline-repair stage. Allowed surfaces are:

- test files and deterministic test-only fixtures/helpers;
- `scripts/**` validation/test runners;
- `package.json` test/validation scripts and the smallest verified production dependency version changes;
- `package-lock.json` generated by the selected package-manager operation;
- validation-only `.github/workflows/ci.yml`;
- `AGENTS.md` and `docs/codex/**` status/risk/roadmap corrections;
- an existing production helper export only if a dynamic test cannot be written without it and runtime call sites remain byte-for-byte/behaviorally unchanged.

Do not change server bootstrap, route/middleware order, public API fields/status/error codes, upload/queue lifecycle, command execution, pricing, profiles, Docker/Compose, deploy workflow, slicer behavior, cleanup, or production topology. Do not begin S1 implementation. No new runtime or test framework dependency is allowed without an explicit `BLOCKED` report and architect decision.

## Workstream A — make validation gates fail closed

### A1. Python test discovery

Replace the direct `unittest discover` package command with a small standard-library runner that:

- discovers the intended `tests/unit/python/test_*.py` suite;
- exits non-zero when discovery finds zero tests;
- exits non-zero on import/discovery errors or test failures;
- reports the actual discovered/run/failed/error/skipped counts without hiding unittest output;
- remains invokable through the existing cross-platform Python launcher and leaves no `__pycache__` or `.pyc` residue in the repository.

Add regression tests proving both a non-empty passing suite and a deliberately empty temporary discovery root. Do not prove this only with source-text assertions.

### A2. Syntax discovery

Make both tracked-source syntax validators fail non-zero if their applicable tracked-file set is empty. Add focused tests for the zero-file condition without modifying Git state or the real tracked file set. Preserve dynamic discovery and exclusions.

### A3. Repository-safety scanner

Tighten the scanner without pretending it is a full history/entropy secret scanner:

- replace broad prefix exemptions such as every value beginning with `test`, `dummy`, or `example` with a narrow exact/path-scoped set of clearly inert sentinels;
- exempt only expressions that are wholly environment/runtime references; a literal combined with `${...}`, `$()`, `process.env`, `os.environ`, or similar must not make the whole value safe;
- do not exempt a hard-coded literal merely because it is wrapped in a function call;
- never print a suspected secret value—only path and stable rule ID;
- retain narrow, documented fixture/size exceptions rather than broad directory exemptions.

Add synthetic regression tests for each bypass class and end-to-end scanner selection. At minimum prove:

- a real-looking value whose text starts with a former placeholder prefix is detected;
- a hard-coded literal plus interpolation is detected;
- a function-wrapped hard-coded literal is detected;
- a pure environment reference and an exact inert sentinel remain allowed;
- private-key marker, `.env`, runtime/private-fixture path policy, and size limit are enforced;
- staged/tracked scope selection and `inspect()` actually inspect intended files. Add an explicit `--require-nonempty` (or equivalently named) mode: tracked CI must use it; staged validation must use it after the intended commit files are staged; an ordinary clean-tree staged scan may still succeed with zero files. Test both allowed-empty and required-nonempty failure behavior.

Construct all test markers from inert fragments where necessary so the test source itself is not mistaken for a committed secret. Do not add realistic credentials.

### A4. CI credential hygiene and portability

- Set `persist-credentials: false` on checkout in the validation-only CI job.
- Keep permissions read-only and keep this workflow separate from deployment.
- Preserve `pull_request`, non-main push, and manual validation triggers; do not claim this protects a direct `main` push.
- Make local Python executable selection work on supported Windows and `python3`-only Linux environments while preserving `TEST_PYTHON_EXECUTABLE` as the explicit override. Test deterministic selection/error behavior without relying on the host PATH layout. Do not invoke a shell or interpolate command strings.

## Workstream B — repair weak characterization evidence

### B1. Live admin timing-safe path

Replace the regex-only proof with an isolated dynamic test that observes the live `requireAdmin` middleware's calls to `crypto.timingSafeEqual`. Prove:

- correct inert key reaches `next()` and invokes the timing-safe primitive;
- wrong equal-length inert key is rejected and invokes it;
- wrong unequal-length inert key is rejected and still invokes the deliberate dummy timing-safe comparison.

The test must fail if the middleware changes to direct `===` comparison while a dead timing-safe helper remains in the file. Avoid timing benchmarks.

### B2. Rejected queue tasks never execute

For both per-client-cap and total-overflow scenarios, instrument the rejected callback in the isolated queue process. Return a `rejectedTaskRuns` count and assert it remains exactly zero after accepted work drains. The test must fail for an implementation that rejects the promise but also enqueues/runs the task.

### B3. Error-code/status ownership

Replace the backward-window source regex with dynamic `handleProcessingError` cases using a fake response, or an equally strong structural test that binds each status and code to the same response branch. Cover the current stable mappings, including adjacent equal-status cases such as invalid source geometry/archive. Preserve behavior; do not expand the public contract in this stage.

### B4. Keep proven runner behavior

Retain the current subprocess tests for all four Python runner entrypoints. Do not weaken or replace the real process-exit proof with static source matching.

## Workstream C — remediate known production dependency advisories

Reproduce the audit before changing dependencies and save only non-sensitive command evidence in the final report. The checkpoint reported one high and three moderate affected package entries. Architect registry recheck classified the advisories as follows, but verify them again rather than treating this list as immutable registry truth:

- Multer `GHSA-72gw-mp4g-v24j` (high, nested field-name denial of service; vulnerable below the registry-reported fixed line);
- Multer `GHSA-3p4h-7m6x-2hcm` (moderate, incomplete aborted-upload cleanup);
- Express/body-parser/qs `GHSA-q8mj-m7cp-5q26` (moderate).

Then:

1. query the configured npm registry for current package metadata/advisory resolution and verify the actual compatible fixed versions; do not rely on memory or invent versions;
2. choose the smallest compatible non-major production dependency update that removes the named advisories. The architect recheck observed candidate direct floors `express: ^4.22.2` and `multer: ^2.2.0`, with transitive `body-parser 1.20.6` and `qs 6.15.3`; use them only if fresh registry/advisory evidence confirms them;
3. keep `express` and `multer` as the direct owners. Do not add direct `qs`/`body-parser` dependencies or an `overrides` entry merely to force the transitive graph;
4. do not use `npm audit fix --force`, suppress audit output, run lifecycle scripts, or update unrelated dependencies for convenience;
5. select and record an exact maintained npm version compatible with the CI Node 20 runtime. Declare that version in repository tooling metadata, use that exact npm version locally to regenerate the lock, and make CI select/verify the same exact npm version before `npm ci`; never use `npm@latest`;
6. update direct minimum ranges when required to prevent lock regeneration from selecting the known-vulnerable line. Regenerate only manifest/lock state with one explicit `--package-lock-only --ignore-scripts --no-audit --no-fund` operation using the verified direct versions, and report the exact command plus Node/npm versions;
7. review manifest and lockfile diffs for unexpected packages, lifecycle scripts, Git/URL dependencies, integrity loss, or major-version changes;
8. run the full deterministic suite and contract tests after the update;
9. add `npm audit --omit=dev --audit-level=moderate` to validation CI only after the same command is green locally.

Acceptance requires all three named advisories absent, zero production moderate/high/critical advisories, and a green `npm audit --omit=dev --audit-level=moderate` both locally and in CI. Capture sanitized JSON evidence proving the three GHSA identifiers are absent; do not commit transient audit output. Also run full `npm audit --omit=dev`; report any low advisory even though low severity alone does not fail this stage. Resolve an unexpected advisory only when a minimal compatible fix is verified and in scope; otherwise stop with `BLOCKED` and preserve a safe first commit if Workstreams A/B are green.

Do not claim that a library update alone fixes the application's upload-residue or request-limit design. Those remain S1/S2 work.

## Workstream D — reconcile durable knowledge and stage ownership

Update the Codex knowledge layer so a future agent cannot confuse the original baseline snapshot with current evidence:

- add a concise, commit-anchored post-S0/S0.1 verification block with exact already-created commit(s), test counts, audit result, and conditional environment skips. A file cannot contain the hash of its own commit: put final documentation in a later commit that names the preceding implementation commits, and put the final documentation commit/HEAD hash only in the handoff report;
- mark `project-map.md` and `security-model.md` matrices explicitly as historical where appropriate, and add/update a current-state delta rather than leaving current controls as `ABSENT`/`UNTESTED` after tests now exist;
- correct the description of `resolveValidatedOutputFile`: it is an existing filesystem-checking helper newly exported for tests, not a pure helper;
- mark multipart parser-limit behavior and `runCommand` argument/environment integrity as `NOT_COVERED_S0`, then give them explicit later exit criteria;
- represent S0 as `VERIFIED` only after this stage's mandatory gates and dependency gate are green. Otherwise keep it `IN_PROGRESS`/`BLOCKED` with precise evidence.

Add these missing risks and exit criteria to the security model/plan:

1. Native Python/slicer subprocesses currently inherit the API process environment unless code evidence proves otherwise. Model parser/slicer compromise, inherited secret exposure (including `ADMIN_API_KEY`), and unrestricted egress. S1 must establish and test a minimal subprocess-environment allowlist; do not implement it here.
2. Public slice endpoints have no application-layer service authentication unless code evidence proves otherwise. Localhost binding/private topology is a separate control. Production promotion is blocked until service auth plus private/egress-restricted topology is verified, or an explicit architect-approved risk decision exists.
3. Model multipart/HTTP ingress resource exhaustion beyond `fileSize`: field/part/header counts and sizes, total upload time, request/header/socket timeouts, and connection limits. Assign exact S1/S2 verification exits.
4. Main push still activates deployment independently of the validation workflow. Branch protection/required checks are external and `UNVERIFIED`; no current commit is promotion authorization.

Resolve roadmap/file ownership so later work can safely parallelize:

- this S0.1 stage is the sole owner of `package.json`, `package-lock.json`, validation scripts, CI validation, and current-state documentation;
- after S0.1 integration, S1a owns upload/job-workspace lifecycle; S1b follows S1a for queue deadlines/abort; S1c follows the AbortSignal contract for process-tree cancellation and subprocess-env minimization;
- S3 build/provenance may run in parallel with S1 runtime work but must not edit manifests/lockfile; it may own Docker/build/validation-deploy separation only;
- S2 artifact lifecycle waits for S1 workspace ownership; S2 container envelope waits for S3 image-control decisions;
- no production promotion before the later auth/topology and deploy-safety gates.

## Mandatory tests and adversarial mutation checks

Use temporary copies/directories for negative tests. Never delete/rename tracked files to simulate failures.

Before committing, demonstrate that the new tests fail for controlled temporary mutations or injected seams representing at least:

- zero Python tests discovered;
- empty JS and Python syntax scopes;
- each scanner bypass listed in A3 and an empty scanner scope;
- direct-equality admin auth with an unused timing-safe helper;
- reject-then-run queue behavior;
- a missing/incorrect adjacent error status mapping.

Restore/discard only the temporary mutation mechanism, then rerun the clean suite. Report the mutation names and expected failures, not secret-like values. Do not mutate production tracked files in the working tree for these checks.

## Validation gates

Report exact commands, exit codes, counts, and relevant versions. Mandatory for the final dependency commit:

1. exact clean preflight and baseline-diff proof;
2. focused tests for every changed component;
3. adversarial mutation checks above;
4. `git diff --check`;
5. complete tracked JavaScript and Python syntax gates, with non-zero-count proof and no cache residue;
6. JavaScript unit suite with exact pass/fail count;
7. Python unit suite with exact discovered/run/pass/fail/skip count;
8. aggregate `npm test`;
9. tracked and staged repository-safety gates plus their end-to-end tests;
10. instruction-mirror consistency;
11. clean install from the final lockfile using `npm ci --ignore-scripts --no-audit --no-fund`, followed by the full gates again;
12. `npm audit --omit=dev --audit-level=moderate` exit `0`, all named GHSAs absent by sanitized JSON evidence, and full production-audit result reported;
13. manifest/lock diff review, `git status --short`, `git diff --stat`, and post-commit clean status.

The dependency update changes an image input even though Dockerfile/Compose behavior is out of scope. Therefore run a local Docker image build and synthetic `/health` smoke if a daemon is available. Use only inert configuration, do not publish the image, and remove only containers/images created by this task after first identifying their exact IDs. If the daemon is unavailable, report `NOT_RUN_ENVIRONMENT`; the dependency commit may remain as a local checkpoint, but the stage is not production-promotion evidence. Real slicer integration and hosted GitHub Actions remain out of scope unless their legal/native prerequisites already exist and a focused offline run is safe; never call a remote slicer or use customer/private models.

## Commit and stop policy

Use at most three local commits:

1. `test: make slicer validation gates fail closed` — Workstreams A/B and provisional risk corrections; allowed only when all non-dependency mandatory gates are green.
2. `chore: remediate production dependency advisories` — minimal manifest/lock/CI audit change; allowed only when clean install, all tests, safety gates, and dependency acceptance criteria are green.
3. `docs: verify S0.1 checkpoint` — final current-state/roadmap reconciliation that names the exact preceding implementation commit hashes and evidence; allowed only after re-running documentation/safety/diff checks. If dependency work is blocked, this optional checkpoint may instead document only the first safe commit and must keep S0 `IN_PROGRESS`.

Never amend, squash, rebase, push, or open a PR. If the dependency gate is blocked after a safe first commit, keep that commit and report `CHECKPOINT_COMMITTED`; do not make the dependency commit. If a mandatory A/B gate is red, make no implementation commit and report `BLOCKED`.

Status meanings for this stage:

- `COMPLETED`: the required implementation commit(s) and final documentation commit exist; all mandatory gates including the moderate-threshold production audit are green; the available local Docker build/health smoke is green; only hosted CI/external branch protection remain unverified.
- `CHECKPOINT_COMMITTED`: every commit made is locally safe, but either dependency resolution is blocked by verified registry/compatibility evidence (in which case do not make the dependency commit), or the dependency commit and local gates are green while Docker validation is unavailable. Report the exact blocker/skip and do not label the overall baseline production-ready.
- `BLOCKED`: preflight is unsafe or a mandatory A/B gate is red; no unsafe commit.

## Required final report

Return exactly this structure in Hungarian:

```text
STATUS: COMPLETED | CHECKPOINT_COMMITTED | BLOCKED
S0_BASELINE:
WORK_BASELINE:
BRANCH:
COMMITS:
MODIFIED_FILES:
ARCHITECT_FINDINGS_VERIFIED:
VALIDATION_GAPS_REPAIRED:
ADVERSARIAL_MUTATION_EVIDENCE:
DEPENDENCY_BEFORE_AFTER:
AUDIT_EVIDENCE:
TESTS_AND_COUNTS:
CI_EVIDENCE:
KNOWLEDGE_STATE_UPDATE:
CONTRACT_PRESERVATION_PROOF:
UNRUN_OR_BLOCKED_GATES:
KNOWN_REMAINING_RISKS:
NEXT_PARALLEL_ETAPS:
FORBIDDEN_SIDE_EFFECTS_CHECK:
```

The final forbidden-side-effects line must explicitly confirm: no push/PR/tag/release, no VPS/deploy, no remote/production application API or slicer call, no customer model/private fixture, no real secret, no LeadPilot change, no pricing/profile/runtime-artifact mutation, no Dockerfile/Compose/deploy-workflow change, and no unrelated worktree damage. Report any disposable local Docker build/health smoke and its cleanup separately.
